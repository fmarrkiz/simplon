'use strict'
/*while( result.length = 1 && result.length < 7) { 
    if (result.length == 1) { result;} else {
    result += result;}

    console.log(result);
    }

let result = '#';
do { 
    console.log(result);
   += result; 
   } while( result.length = 1 && result.length < 7);

// works but starts at '##'
while( result.length = 1 && result.length < 8) {
    result += '#';
        console.log(result);
} 
// option 1
let result = '#';
if(result.length = 1) {
    console.log(result);} 
    else { while( result.length = 1 && result.length < 7) {
    result += '#';
        console.log(result);}
}       
//option 2
let result = 'x';

if(result.length = 1) {
  alert('x');} 



    else { while( result.length = 1 && result.length < 7) {
    result += '#';
        console.log(result);}
}       
        */