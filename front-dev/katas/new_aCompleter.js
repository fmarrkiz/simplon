a = Math.PI;

let Obje = {
    name : a,

}

console.log((Obje.))

/*
function toString(a){
if (typeof a == 'number' ){
    return `${a}`;
  } else if (typeof a == 'object') { 
    return `[${a}]`;
  } else if ( typeof a == 'boolean') {
    return `${a}`;
  }
}

objet = (a).getProtypeOf();

console.log(objet);

 /*

  function ToString() {
  toString :
    if (typeof a == 'number' ) {
        return `"${a}"` ;
      } else if (typeof a == 'object') { 
        return `[${a}]`;
      } else if ( typeof a == 'boolean') {
        return `"${a}"`;
      }

  }

  console.log(true.ToString());
  */