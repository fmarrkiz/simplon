
/* is_busy = true;

function getStatus(is_busy) {
 
    this.status =  {
     is_busy ? this.status = 'busy' : 'available';  }
   }

console.log(getStatus(is_busy));

----

const frequency = (arr, item) => {
    let count = 0;
    let ArrayOfCounts = [];
    
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === item) {
            count++;
        }
    }
    return count;
};

if ( count % 2 === ) {
    return 
}


console.log (likes(names));

*/

arr = ["a", "k", "t", "t", "k", "K", 2, 3]

let newArr = arr.sort();
for (let i = 0; i < arr.length ; i++ ) {
    
if ( a[i] === a[i + 1]) {

}
}

